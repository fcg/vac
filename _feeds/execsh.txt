#!/bin/bash

find . -name "*.sh" -type f -exec chmod +x {} \;

find . -name "*.sh" -type f -exec sh -c 'cd "$(dirname "$1")" && ./$(basename "$1")' sh {} \;

find . \( -name "*.html" -o -name "*.md" -o -name "*.sh" \) -type f -delete
